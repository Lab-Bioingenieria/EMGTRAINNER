# Private storage directory

This directory is reserved for local EMG/session captures, participant exports, label aggregates, trained models, and other runtime artifacts.

The contents are intentionally ignored by Git and Docker build contexts. Do not commit real data here. See `../../docs/data-governance.md` for the private storage workflow, backup boundary, and history-rewrite guidance.

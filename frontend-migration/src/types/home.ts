export interface CardHomeProps {
    title: string
    subtitle: string
    description: string
    to: string
    // Aceptamos el icono como un componente o string, pero se recomienda usar el slot 'icon'
}
